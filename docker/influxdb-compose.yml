services:
  influxdb:
    image: influxdb:2.8.0
    container_name: solark-influxdb
    restart: unless-stopped

    ports:
      - "8086:8086"

    volumes:
      - ./data:/var/lib/influxdb2
      - ./config:/etc/influxdb2
